import 'package:flutter/material.dart';

class LabThree extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('test purple')),
      body: Container(
        color: Colors.purple,
      ),
    );
  }
}
