import 'package:flutter/material.dart';

class LabTwo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('test blue')),
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
